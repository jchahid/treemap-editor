---
name: user-preferences
description: User is French-speaking, Angular expert, prefers Signals + Tailwind + standalone components
metadata:
  type: user
---

User works in French (UI copy in French). Comfortable with Angular 17+ APIs.

Explicitly requested:
- Angular Signals for local state (`signal`, `computed`, `input`, `output`)
- Tailwind CSS for new UI pages (login, dashboard)
- Standalone components (no NgModules)
- Clean, modern design — gradient indigo brand color (#4F46E5 / #6366F1)

**How to apply:** Always use `signal()` for mutable state in new components. Use `input.required<T>()` and `output<T>()` for component I/O. Tailwind utility classes for layout/spacing on new pages; CSS variables (var(--accent) etc.) for the existing editor components.
